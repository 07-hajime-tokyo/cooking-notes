# AGENTS.md

このサイトは、レシピと加熱時間をまとめた個人用メモです（旧「エアフライヤー加熱時間メモ」／旧名 `air-fryer-times`）。
2026-08-09に `cooking-notes` へ改名し、扱う範囲をエアフライヤーから料理全般へ広げました。

## 作業方針

- GitHubを正本、Vercelを閲覧場所として扱う。
- 掲載内容は `data.js` に集約する。`app.js` にレシピ本文を書かない。
- レシピは「材料・手順・押さえるポイント」の3点をそろえる。温度と時間だけのものは `quickTable` 側へ。
- 温度帯や時間には理由を書く（例: 65℃を超えると麹の酵素が失活する）。手順だけ並べない。
- 医療・安全情報として断定しすぎず、中心温度と短め加熱からの追加を優先して書く。
- 情報密度を優先する。余白と装飾より一覧性。フィルタと開閉状態は localStorage で復元する。
- 内容を変えたら `README.md` の説明とズレていないか確認する。
- 秘密情報や個人情報は入れない。

## 公開

1. `npm run check`
2. `npm start` でローカル表示確認（レシピ／早見表の両タブ）
3. `git status -sb`
4. commit / push
5. `vercel --prod --yes`
